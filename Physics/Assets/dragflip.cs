using UnityEngine;

public class dragflip : MonoBehaviour {


    // Start is called before the first frame update
    void Start() {
        Debug.Log("Hi, das ist mein erstes Hello World!!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
